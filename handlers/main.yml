---
# handlers file for ansible-haya
- name: restart bootnode
  docker_container:
    name: "{{ bc_name }}-boot"
    restart: true

- name: restart prodnode
  docker_container:
    name: "{{ bc_name }}-prod"
    restart: true
